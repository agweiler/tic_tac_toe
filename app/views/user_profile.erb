<h1>Welcome to your profile page!<h1>

<p><%= @user.email %><p>

  <!-- insert code calling the scores (win or loss) of previous games. I.e. 1. List games_users.game_id where @user.id = games_users.user_id; 2. For each game_id, if @user.id = games.winner_id puts "won", else puts "loss".
     ************************************************ -->


